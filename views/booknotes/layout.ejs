<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/bootstrap.min.css">
    <link rel="stylesheet" href="/all.min.css"> <style>
        body {
            display: flex;
            min-height: 100vh;
            overflow-x: hidden;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol";
            background-color: #f8f9fa;
        }
        .content {
            flex-grow: 1;
            padding: 1.5rem;
            background-color: #ffffff;
            border-radius: .375rem;
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
            margin: 1.5rem;
        }

        /* 表单和按钮样式 (可以根据你的现有样式调整) */
        .form-group label { font-weight: 600; margin-bottom: .5rem; color: #495057; }
        .form-control { border-radius: .25rem; border: 1px solid #ced4da; padding: .6rem .75rem; font-size: .9rem; }
        .form-control:focus { border-color: #80bdff; box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25); }
        .btn { border-radius: .25rem; font-size: .9rem; padding: .6rem 1rem; }
        .btn-primary { background-color: #007bff; border-color: #007bff; }
        .btn-primary:hover { background-color: #0056b3; border-color: #004085; }
        .btn-secondary { background-color: #6c757d; border-color: #6c757d; }
        .btn-secondary:hover { background-color: #5a6268; border-color: #545b62; }
        .btn-danger { background-color: #dc3545; border-color: #dc3545; }
        .btn-danger:hover { background-color: #c82333; border-color: #bd2130; }
        .btn-warning { background-color: #ffc107; border-color: #ffc107; color: #212529;}
        .btn-warning:hover { background-color: #e0a800; border-color: #d39e00; }
        .btn-success { background-color: #28a745; border-color: #28a745; }
        .btn-success:hover { background-color: #218838; border-color: #1e7e34; }


        /* 表格样式 (可以根据你的现有样式调整) */
        .table { margin-bottom: 1rem; border-collapse: separate; border-spacing: 0; border-radius: .375rem; overflow: hidden; }
        .table thead th { background-color: #343a40; color: #fff; border-bottom: 2px solid #dee2e6; padding: .75rem 1rem; vertical-align: bottom; font-weight: 600; }
        .table tbody tr { transition: background-color 0.15s ease-in-out; }
        .table-striped tbody tr:nth-of-type(odd) { background-color: rgba(0, 0, 0, .03); }
        .table-hover tbody tr:hover { background-color: rgba(0, 0, 0, .06); }
        .table td { padding: .75rem 1rem; vertical-align: middle; border-top: 1px solid #dee2e6; }
        .table-responsive { border-radius: .375rem; overflow-x: auto; }

        h1, h2 { color: #343a40; margin-bottom: 1rem; }
        hr { border-top: 1px solid rgba(0, 0, 0, .1); margin-top: 1.5rem; margin-bottom: 1.5rem; }

        /* 移动端适配 */
        @media (max-width: 767.98px) {
            .content { margin: 1rem; }
            .form-row > [class*="col-"] { margin-bottom: 1rem; }
            .form-row > [class*="col-"]:last-child { margin-bottom: 0; }
            .col-12 button.btn { width: 100%; }
        }
        .navbar-toggler { margin-bottom: 1rem; margin-left: 0; }
    </style>
</head>
<body>
    <%- include('../menu/sidebar.ejs') %> <div class="content">
        <button class="navbar-toggler d-md-none mb-3 btn btn-outline-secondary" type="button" data-toggle="collapse" data-target="#sidebarCollapse" aria-controls="sidebarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span> 菜单
        </button>

        <h1><%= pageTitle %></h1>
        <hr>
        <%- body %> </div>

    <script src="/jquery-3.5.1.slim.min.js"></script>
    <script src="/popper.min.js"></script>
    <script src="/bootstrap.min.js"></script>
</body>
</html>