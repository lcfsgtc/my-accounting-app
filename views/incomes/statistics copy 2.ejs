<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>收入统计</title>
    <link rel="stylesheet" href="/bootstrap.min.css">
    <link rel="stylesheet" href="/all.min.css">
    <style>
        /* 这里放置statistics.ejs页面特有的样式 */

        /* 为了确保侧边栏和主内容布局正确，这些核心布局样式通常需要放在body或最外层div上 */
        /* 如果您sidebar.ejs中已经包含了这些样式，这里可以省略，但通常建议放在主布局文件中 */
        body {
            display: flex; /* 使用 flex 布局实现侧边栏和主内容 */
            min-height: 100vh; /* 确保最小高度填充视口 */
            overflow-x: hidden; /* 防止水平滚动条出现 */
            /* padding-top: 20px; 如果不再需要顶部的固定间距，可以移除或调整 */
        }
        .content {
            flex-grow: 1; /* 主内容区域占据剩余空间 */
            padding: 1.5rem; /* 为内容添加内边距 */
        }

        /* 优化表单和表格样式 (Bootstrap 5) */
        .form-label {
            margin-bottom: .5rem;
        }
        .form-control, .form-select {
            border-radius: .25rem;
        }
        .btn {
            border-radius: .25rem;
        }
        .table {
            margin-bottom: 1rem;
        }
        .table thead th {
            background-color: #343a40; /* 深色表头 */
            color: #fff;
            border-bottom: 2px solid #dee2e6;
        }
        .table-striped tbody tr:nth-of-type(odd) {
            background-color: rgba(0, 0, 0, .05);
        }
        .table-hover tbody tr:hover {
            background-color: rgba(0, 0, 0, .075);
        }

        /* 响应式调整：小屏幕下表单列的间距和对齐 */
        @media (max-width: 767.98px) {
            /* 针对 Bootstrap 5 的 .row .g-* 布局 */
            .row > div[class*="col-"] { /* 匹配所有 .col-* 类的直接子div */
                margin-bottom: 1rem; /* 增加底部间距 */
            }
            /* 确保按钮在小屏幕上占满宽度 */
            .col-12 button.btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <%- include('../menu/sidebar.ejs') %>

    <div class="content">
        <button class="navbar-toggler d-md-none mb-3 btn btn-outline-secondary" type="button" data-bs-toggle="offcanvas" data-bs-target="#sidebarOffcanvas" aria-controls="sidebarOffcanvas">
            <span class="navbar-toggler-icon"></span> 菜单
        </button>

        <h1 class="mb-4">收入统计</h1>
        <a href="/incomes" class="btn btn-secondary mb-4">
            <i class="fas fa-arrow-left me-2"></i>返回收入列表
        </a>
        <hr>
        <form action="/incomes/statistics" method="GET" class="mb-4">
            <div class="row g-3"> <div class="col-12 col-sm-6 col-md-2">
                    <label for="startDate" class="form-label">开始日期:</label>
                    <input type="date" class="form-control" id="startDate" name="startDate" value="<%= startDate %>">
                </div>
                <div class="col-12 col-sm-6 col-md-2">
                    <label for="endDate" class="form-label">结束日期:</label>
                    <input type="date" class="form-control" id="endDate" name="endDate" value="<%= endDate %>">
                </div>
                <div class="col-12 col-sm-6 col-md-2">
                    <label for="categoryType" class="form-label">类别:</label>
                    <select id="categoryType" name="categoryType" class="form-select">
                        <option value="" <%= categoryType === "" ? 'selected' : '' %>>所有类别</option>
                        <option value="category" <%= categoryType === "category" ? 'selected' : '' %>>大类</option>
                        <option value="subcategory" <%= categoryType === "subcategory" ? 'selected' : '' %>>小类</option>
                    </select>
                </div>
                <div class="col-12 col-sm-6 col-md-2">
                    <label for="minAmount" class="form-label">最小金额:</label>
                    <input type="number" class="form-control" id="minAmount" name="minAmount" value="<%= minAmount %>" step="0.01">
                </div>
                <div class="col-12 col-sm-6 col-md-2">
                    <label for="maxAmount" class="form-label">最大金额:</label>
                    <input type="number" class="form-control" id="maxAmount" name="maxAmount" value="<%= maxAmount %>" step="0.01">
                </div>
                <div class="col-12 col-sm-6 col-md-2">
                    <label for="period" class="form-label">统计时间:</label>
                    <select id="period" name="period" class="form-select">
                        <option value="" <%= period === "" ? 'selected' : '' %>>总计</option>
                        <option value="year" <%= period === "year" ? 'selected' : '' %>>按年</option>
                        <option value="month" <%= period === "month" ? 'selected' : '' %>>按月</option>
                    </select>
                </div>
                <div class="col-12 col-md-2 d-flex align-items-end">
                    <button type="submit" class="btn btn-primary w-100">
                        <i class="fas fa-play-circle me-2"></i>生成统计
                    </button>
                </div>
            </div>
        </form>
        <hr>
        <h2 class="mb-3">统计结果</h2>
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>类别/时间</th>
                        <th>总金额</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (statistics && statistics.length > 0) { %>
                        <% statistics.forEach(item => { %>
                            <tr>
                                <td>
                                    <% if (period === 'year' && item._id) { %>
                                        <%= item._id %> 年
                                    <% } else if (period === 'month' && item._id) { %>
                                        <% if (typeof item._id === 'object' && item._id !== null && item._id.year && item._id.month) { %>
                                            <%= item._id.year %>年<%= String(item._id.month).padStart(2, '0') %>月
                                        <% } else { %>
                                            <%= item._id %>
                                        <% } %>
                                    <% } else if (categoryType === 'category' || categoryType === 'subcategory') { %>
                                        <%= item._id %>
                                    <% } else { %>
                                        总计
                                    <% } %>
                                </td>
                                <td>¥ <%= item.totalAmount ? item.totalAmount.toFixed(2) : '0.00' %></td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="2" class="text-center text-muted py-4">没有符合条件的统计数据。</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>

    <script src="/bootstrap.bundle.min.js"></script>
</body>
</html>