<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>收入统计 - 财务管理系统</title>
    <link rel="stylesheet" href="/bootstrap.min.css">
    <link rel="stylesheet" href="/all.min.css">
    <style>
        /* 全局布局样式：确保侧边栏和主内容正确布局 */
        body {
            display: flex;
            min-height: 100vh;
            overflow-x: hidden; /* 防止水平滚动 */
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol";
            background-color: #f8f9fa; /* 轻微的背景色 */
        }
        .content {
            flex-grow: 1; /* 主内容区域占据剩余空间 */
            padding: 1.5rem; /* 增加内边距 */
            background-color: #ffffff; /* 内容区背景色 */
            border-radius: .375rem; /* 轻微圆角 */
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075); /* 轻微阴影 */
            margin: 1.5rem; /* 整体外边距，使其不紧贴屏幕边缘 */
        }

        /* 统一表单元素样式 (Bootstrap 4 兼容) */
        .form-group label { /* Bootstrap 4 的 label 通常在 .form-group 内 */
            font-weight: 600;
            margin-bottom: .5rem;
            color: #495057;
        }
        .form-control { /* Bootstrap 4 的 select 也是 form-control */
            border-radius: .25rem;
            border: 1px solid #ced4da;
            padding: .6rem .75rem; /* 稍微增加内填充 */
            font-size: .9rem;
        }
        .form-control:focus {
            border-color: #80bdff;
            box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
        }
        .btn {
            border-radius: .25rem;
            font-size: .9rem;
            padding: .6rem 1rem;
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }
        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #004085;
        }
        .btn-secondary {
            background-color: #6c757d;
            border-color: #6c757d;
        }
        .btn-secondary:hover {
            background-color: #5a6268;
            border-color: #545b62;
        }

        /* 表格样式 */
        .table {
            margin-bottom: 1rem;
            border-collapse: separate; /* 允许圆角边框 */
            border-spacing: 0;
            border-radius: .375rem; /* 表格整体圆角 */
            overflow: hidden; /* 隐藏超出圆角的内容 */
        }
        .table thead th {
            background-color: #343a40; /* 深色表头 */
            color: #fff;
            border-bottom: 2px solid #dee2e6;
            padding: .75rem 1rem;
            vertical-align: bottom;
            font-weight: 600;
        }
        .table tbody tr {
            transition: background-color 0.15s ease-in-out;
        }
        .table-striped tbody tr:nth-of-type(odd) {
            background-color: rgba(0, 0, 0, .03); /* 更浅的条纹 */
        }
        .table-hover tbody tr:hover {
            background-color: rgba(0, 0, 0, .06); /* 鼠标悬停效果增强 */
        }
        .table td {
            padding: .75rem 1rem;
            vertical-align: middle;
            border-top: 1px solid #dee2e6;
        }
        .table-responsive {
            border-radius: .375rem; /* 响应式表格的圆角 */
            overflow-x: auto; /* 确保小屏幕下可滚动 */
        }

        /* 标题和分隔线 */
        h1, h2 {
            color: #343a40;
            margin-bottom: 1rem;
        }
        hr {
            border-top: 1px solid rgba(0, 0, 0, .1);
            margin-top: 1.5rem;
            margin-bottom: 1.5rem;
        }

        /* 响应式表单布局调整 (Bootstrap 4 兼容) */
        @media (max-width: 767.98px) {
            .content {
                margin: 1rem; /* 小屏幕下调整外边距 */
            }
            /* Bootstrap 4 的 form-row */
            .form-row > [class*="col-"] {
                margin-bottom: 1rem; /* 为每个表单项增加底部间距 */
            }
            .form-row > [class*="col-"]:last-child {
                margin-bottom: 0; /* 最后一个表单项不需要底部间距 */
            }
            /* 按钮在小屏幕上占满宽度 */
            .col-12 button.btn {
                width: 100%;
            }
        }
        /* 移动端汉堡菜单按钮位置 */
        .navbar-toggler {
            margin-bottom: 1rem;
            margin-left: 0; /* 确保在内容区左侧 */
        }
    </style>
</head>
<body>
    <%- include('../menu/sidebar.ejs') %>

    <div class="content">
        <button class="navbar-toggler d-md-none mb-3 btn btn-outline-secondary" type="button" data-toggle="collapse" data-target="#sidebarCollapse" aria-controls="sidebarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span> 菜单
        </button>
        <h1 class="mb-4">收入统计</h1>
        <a href="/incomes" class="btn btn-secondary mb-4">
            <i class="fas fa-arrow-left me-2"></i>返回收入列表
        </a>
        <hr>
        <form action="/incomes/statistics" method="GET" class="mb-4">
            <div class="form-row"> <div class="form-group col-12 col-sm-6 col-md-2"> <label for="startDate">开始日期:</label>
                    <input type="date" class="form-control" id="startDate" name="startDate" value="<%= startDate || '' %>">
                </div>
                <div class="form-group col-12 col-sm-6 col-md-2">
                    <label for="endDate">结束日期:</label>
                    <input type="date" class="form-control" id="endDate" name="endDate" value="<%= endDate || '' %>">
                </div>
                <div class="form-group col-12 col-sm-6 col-md-2">
                    <label for="categoryType">类别:</label>
                    <select id="categoryType" name="categoryType" class="form-control"> <option value="" <%= categoryType === "" ? 'selected' : '' %>>所有类别</option>
                        <option value="category" <%= categoryType === "category" ? 'selected' : '' %>>大类</option>
                        <option value="subcategory" <%= categoryType === "subcategory" ? 'selected' : '' %>>小类</option>
                    </select>
                </div>
                <div class="form-group col-12 col-sm-6 col-md-2">
                    <label for="minAmount">最小金额:</label>
                    <input type="number" class="form-control" id="minAmount" name="minAmount" value="<%= minAmount || '' %>" step="0.01" placeholder="例如：100.00">
                </div>
                <div class="form-group col-12 col-sm-6 col-md-2">
                    <label for="maxAmount">最大金额:</label>
                    <input type="number" class="form-control" id="maxAmount" name="maxAmount" value="<%= maxAmount || '' %>" step="0.01" placeholder="例如：1000.00">
                </div>
                <div class="form-group col-12 col-sm-6 col-md-2">
                    <label for="period">统计时间:</label>
                    <select id="period" name="period" class="form-control"> <option value="" <%= period === "" ? 'selected' : '' %>>总计</option>
                        <option value="year" <%= period === "year" ? 'selected' : '' %>>按年</option>
                        <option value="month" <%= period === "month" ? 'selected' : '' %>>按月</option>
                    </select>
                </div>
                <div class="col-12 col-md-2 d-flex align-items-end mt-sm-3 mt-md-0"> <button type="submit" class="btn btn-primary w-100"> <i class="fas fa-chart-bar me-2"></i>生成统计
                    </button>
                </div>
            </div>
        </form>
        <hr>
        <h2 class="mb-3">统计结果</h2>
        <div class="table-responsive"> <table class="table table-striped table-hover">
                <thead class="thead-dark"> <tr>
                        <th>类别/时间</th>
                        <th>总金额</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (statistics && statistics.length > 0) { %>
                        <% statistics.forEach(item => { %>
                            <tr>
                                <td>
                                    <% if (period === 'year' && item._id) { %>
                                        <%= item._id %> 年
                                    <% } else if (period === 'month' && item._id) { %>
                                        <% // 检查 item._id 是否为对象且包含 year 和 month 属性 %>
                                        <% if (typeof item._id === 'object' && item._id !== null && item._id.year && item._id.month) { %>
                                            <%= item._id.year %>年<%= String(item._id.month).padStart(2, '0') %>月
                                        <% } else { %>
                                            <%= item._id %> <% } %>
                                    <% } else if (categoryType === 'category' || categoryType === 'subcategory') { %>
                                        <%= item._id %>
                                    <% } else { %>
                                        总计
                                    <% } %>
                                </td>
                                <td>¥ <%= item.totalAmount ? item.totalAmount.toFixed(2) : '0.00' %></td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="2" class="text-center text-muted py-4">没有符合条件的统计数据。</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>

    <script src="/jquery-3.5.1.slim.min.js"></script>
    <script src="/popper.min.js"></script>
    <script src="/bootstrap.min.js"></script>
</body>
</html>