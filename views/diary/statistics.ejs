<!DOCTYPE html>
<html>
<head>
    <title>日记统计</title>
    <!--link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"-->
    <link rel="stylesheet" href="/bootstrap.min.css">     
    <style>
        body {
            padding-top: 20px;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>日记统计</h1>
    <a href="/diary" class="btn btn-secondary">返回日记列表</a>
    <hr>
    <form action="/diary/statistics" method="GET">
        <div class="form-group">
            <label for="startDate">开始日期:</label>
            <input type="date" class="form-control" id="startDate" name="startDate" value="<%= startDate %>">
        </div>
        <div class="form-group">
            <label for="endDate">结束日期:</label>
            <input type="date" class="form-control" id="endDate" name="endDate" value="<%= endDate %>">
        </div>
        <div class="form-group">
            <label for="mood">心情:</label>
            <select class="form-control" id="mood" name="mood">
                <option value="">所有心情</option>
                <option value="happy" <%= mood === "happy" ? 'selected' : '' %>>快乐</option>
                <option value="sad" <%= mood === "sad" ? 'selected' : '' %>>悲伤</option>
                <option value="neutral" <%= mood === "neutral" ? 'selected' : '' %>>平静</option>
                <option value="excited" <%= mood === "excited" ? 'selected' : '' %>>激动</option>
                <option value="frustrated" <%= mood === "frustrated" ? 'selected' : '' %>>沮丧</option>
            </select>
        </div>
        <button type="submit" class="btn btn-primary">生成统计</button>
    </form>
    <hr>
    <h2>统计结果</h2>
    <table class="table">
        <thead>
        <tr>
            <th>统计项</th>
            <th>值</th>
        </tr>
        </thead>
        <tbody>
        <tr>
            <td>总日记数</td>
            <td><%= statistics[0] ? statistics[0].totalDiaries : 0 %></td>
        </tr>
         <tr>
            <td>包含几种心情</td>
            <td><%= statistics[0] ? statistics[0].totalMoods.length : 0 %></td>
        </tr>
        <tr>
            <td>包含心情列表</td>
            <td><%= statistics[0] ? statistics[0].totalMoods : 0 %></td>
        </tr>
        </tbody>
    </table>
</div>
</body>
</html>